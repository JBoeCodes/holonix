# Sync to Git

Add, commit, and push all changes to the holonix.git repository.

## Usage
```
/sync
```

## Description
This command will:
1. Add all changes to git staging
2. Create a commit with a descriptive message
3. Push changes to the origin/main branch

The commit message will include details about what was changed and is marked as generated with Claude Code.